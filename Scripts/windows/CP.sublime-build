{
	"shell_cmd": "g++ ${file} -o ${file_base_name} && ${file_base_name} <input.in> output.out",
	"working_dir": "${file_path}",
    "selector": "source.c, source.c++, source.cpp, source.cxx",
    "shell": "true",
    "file_regex": "^(..[^:]):([0-9]+):?([0-9]+)?:? (.)$"
}
