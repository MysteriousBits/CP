{
    "shell_cmd": "g++ -Wall -std=c++17 -DLOCAL ${file} -o ${file_base_name} && ${file_base_name} <input.in> output.out 2> error.log",
    "working_dir": "${file_path}",
    "selector": "source.c, source.c++, source.cpp, source.cxx",
    "shell": "true",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$"
}
